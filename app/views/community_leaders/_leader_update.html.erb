<%= form_for @leader, :url => url_for(:controller => 'community_leaders', :action => 'update') do |f| %>
  <fieldset>
    <legend>Community Leaders</legend>
    <% if @leader.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@leader.errors.count, "error") %> prohibited this community_leaders from being saved:</h2>

        <ul>
        <% @leader.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


    <div class="field">
      <%= f.label :community_leaders_first_name %><br>
      <%= f.text_field :fname %>
    </div>
    <div class="field">
      <%= f.label :community_leaders_last_name %><br>
      <%= f.text_field :lname %>
    </div>
    <div class="field">
      <%= f.label :home_address %><br>
      <%= f.text_field :address %>
    </div>
    <div class="field">
      <%= f.label :zip_code %><br>
      <%= f.number_field :zip_code %>
    </div>
    <div class="field">
      <%= f.label :phone %><br>
      <%= f.number_field :phone %>
    </div>
    <div class="field">
      <%= f.label :logo %><br>
      <%= f.file_field :logo %>
    </div>
    <div class="actions">
      <%= f.submit :Submit, class: "button" %>
    </div>
  </fieldset>
<% end %>