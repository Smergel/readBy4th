<div class="row"
<div class="large-12 columns">


<%= form_for @story, url: "/story", html: {method: "POST"} do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :link_to_article %><br>
    <%= f.text_field :story %>
  </div>
  <div class="actions">
    <%= f.submit :Submit, class: "button" %>
  </div>
<% end %>
</div>
</div>
